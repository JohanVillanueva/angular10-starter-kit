// This file will be replaced (in build process) by its peer according to the environment [dev, prod, local, qa, uat, etc.]

import { Environment } from './environment.model';

export const environment: Environment = {
  production: false,
};
